package frontend;

/**
 * Enum representing the type of highlight applied to a cell.
 * Used for visual indication of valid actions such as movement and building.
 */
public enum HighlightType {
    NONE,
    MOVE,
    BUILD,
    ARTIFACT_USABLE,

}
